<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tk.brianvalente.whatsappmd" >

    <uses-sdk tools:overrideLibrary="com.getbase.floatingactionbutton, com.mikepenz.materialdrawer, com.mikepenz.iconics" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme" >
        <activity
            android:name=".WhatsAppMD"
            android:label="@string/app_name" >

        </activity>
        <activity
            android:name=".FABSettings"
            android:label="FAB Buttons" >
        </activity>
        <activity
            android:name=".MaterialDrawer"
            android:label="@string/title_activity_material_drawer"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".test"
            android:label="Floating Action Button"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
